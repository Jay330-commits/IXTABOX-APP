/**
 * AuthService class
 * Note: Authentication is handled entirely by Supabase.
 * Password hashing/verification is handled automatically by Supabase Auth.
 * This service is kept for potential future utility methods if needed.
 */
export class AuthService {
  // All authentication operations (including password handling) are managed by Supabase
}
